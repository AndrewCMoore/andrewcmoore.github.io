# Welcome

This is a docs viewer for project documentation. It lists every `.md` file in this
`docs/` folder automatically — no build step.

## Adding a new doc

1. Add a `.md` file to `docs/`.
2. Commit and push.
3. Refresh the page — it appears in the sidebar.
